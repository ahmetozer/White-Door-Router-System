#!/bin/sh

#if [ "$1" != "${1#*[0-9].[0-9].[0-9].[0-9]}" ]; then
if [ "$1" != "${1#*[0-9].[0-9]}" ]; then
  echo IPv4
elif [ "$1" != "${1#*:[0-9a-fA-F]}" ]; then
  echo IPv6
elif [ "$1" != "${1#*[0-9a-zA-Z].[a-zA-Z]}" ]; then
  echo Name
else
  echo "Unrecognized IP format '$1'"
fi
